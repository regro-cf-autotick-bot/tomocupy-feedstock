{% set version = "1.0" %}
{% set build = 0 %}

package:
  name: tomocupy
  version: {{ version }}

source:
  url: https://github.com/tomography/tomocupy/archive/refs/tags/v{{ version }}.tar.gz
  sha256: c280472e81ac90147680a1d1d71e312c69c47428e9b2e6731e759ef40cff5140

build:
  skip: True  # [cuda_compiler_version == "10.2"]
  skip: True  # [cuda_compiler_version in (undefined, "None")]
  skip: True  # [not win]
  skip: True  # [cuda_compiler_version != "11.2"]
  number: 2
  script: {{ PYTHON }} -m pip install . -vv
  script_env:
    # - CUDACXX="%BUILD_PREFIX%\Library\bin\nvcc.bat"  # [win]
    - CMAKE_GENERATOR=Ninja  # [not win]
  # Minimum supported arch of this package is 60
    - CUDAARCHS=60;72;75
requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('cuda') }}  # [cuda_compiler_version != "None"]
    - cmake
    - ninja  # [not win]
    - swig
  host:
    - cudatoolkit  # [cuda_compiler_version != "None"]
    - numpy
    - pip
    - python
    - scikit-build
    - setuptools
  run:
    - python
    - cupy
    - h5py
    - numexpr
    - numpy
    - opencv
    - pywavelets
    - tifffile

test:
   requires:
     - pip
   imports:
# Conda-forge bots do not have GPUs for testing
#      - tomocupy
   commands:
     - pip check
# Conda-forge bots do not have GPUs for testing
#      - tomocupy --help

about:
    home: https://github.com/tomography/tomocupy
    license: BSD-3-Clause
    license_family: BSD
    license_file: .licenses/license.c
    summary: 'GPU-based reconstruction of tomography data'

extra:
  recipe-maintainers:
    - nikitinvv